name: Client logic tests
on: [push, pull_request, workflow_dispatch]
permissions:
  contents: read
jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x
      - run: dotnet run --project tests/KellysTALONPublicUI.LogicTests.csproj -c Release
      - run: dotnet run --project tests/Bindings/Bindings.Tests.csproj -c Release
